[tox]
envlist = py38

[testenv]
deps=-r{toxinidir}/requirements.txt
     -r{toxinidir}/test-requirements.txt

commands=
   python -m pytest awm/test --cov=awm --cov-report=term

[flake8]
max-line-length = 120